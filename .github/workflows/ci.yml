name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  DOTNET_VERSION: '8.0.x'
  RUST_VERSION: 'stable'

jobs:
  # Build native library for each platform
  build-native:
    name: Build Native (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            rid: linux-x64
            artifact: liboxidize_pdf_ffi.so
          - os: windows-latest
            target: x86_64-pc-windows-gnu
            rid: win-x64
            artifact: oxidize_pdf_ffi.dll
          - os: macos-latest
            target: x86_64-apple-darwin
            rid: osx-x64
            artifact: liboxidize_pdf_ffi.dylib
          - os: macos-latest
            target: aarch64-apple-darwin
            rid: osx-arm64
            artifact: liboxidize_pdf_ffi.dylib
          - os: ubuntu-latest
            target: x86_64-unknown-linux-musl
            rid: linux-musl-x64
            artifact: liboxidize_pdf_ffi.so
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            rid: linux-arm64
            artifact: liboxidize_pdf_ffi.so
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-musl
            rid: linux-musl-arm64
            artifact: liboxidize_pdf_ffi.so
          - os: windows-11-arm
            target: aarch64-pc-windows-msvc
            rid: win-arm64
            artifact: oxidize_pdf_ffi.dll

    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
          targets: ${{ matrix.target }}

      - name: Install musl tools
        if: contains(matrix.target, 'musl')
        run: sudo apt-get update && sudo apt-get install -y musl-tools

      - name: Cache Cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            native/target/
          # runner.arch + target keep the x86_64 and ARM64 caches separate;
          # otherwise an x86_64 ~/.cargo/bin/cargo clobbers the ARM runner's
          # cargo and the build fails with "Exec format error".
          key: ${{ runner.os }}-${{ runner.arch }}-cargo-${{ matrix.target }}-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ runner.arch }}-cargo-${{ matrix.target }}-

      - name: Build Native Library
        working-directory: native
        run: cargo build --release --target ${{ matrix.target }}
        env:
          # musl defaults to static crt, which is incompatible with a cdylib (.so).
          RUSTFLAGS: ${{ contains(matrix.target, 'musl') && '-C target-feature=-crt-static' || '' }}

      - name: Upload Native Artifact
        uses: actions/upload-artifact@v4
        with:
          name: native-${{ matrix.rid }}
          path: native/target/${{ matrix.target }}/release/${{ matrix.artifact }}
          retention-days: 7

  # Build and test .NET wrapper
  test-dotnet:
    name: Test .NET (${{ matrix.os }})
    needs: build-native
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            rid: linux-x64
          - os: ubuntu-24.04-arm
            rid: linux-arm64
          - os: windows-latest
            rid: win-x64
          - os: macos-latest
            rid: osx-arm64

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Download Native Library
        uses: actions/download-artifact@v4
        with:
          name: native-${{ matrix.rid }}
          path: dotnet/OxidizePdf.NET/runtimes/${{ matrix.rid }}/native/

      - name: Restore .NET Dependencies
        working-directory: dotnet
        run: dotnet restore

      - name: Build .NET Wrapper
        working-directory: dotnet
        run: dotnet build --no-restore --configuration Release

      - name: Run .NET Tests
        working-directory: dotnet
        run: dotnet test --no-build --configuration Release --verbosity normal

  # Lint and format checks
  lint:
    name: Lint and Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: ${{ env.RUST_VERSION }}
          components: rustfmt, clippy

      - name: Check Rust Formatting
        working-directory: native
        run: cargo fmt --all -- --check

      - name: Run Clippy
        working-directory: native
        run: cargo clippy --all-targets --all-features -- -D warnings

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Check .NET Formatting
        working-directory: dotnet
        run: dotnet format --verify-no-changes --verbosity diagnostic

  # Security audit
  security:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cargo-audit
        run: cargo install cargo-audit

      - name: Run Cargo Audit
        working-directory: native
        run: cargo audit
